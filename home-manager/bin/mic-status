#!/usr/bin/env sh
if [ "$(pactl get-source-mute @DEFAULT_SOURCE@)" = "Mute: yes" ]; then
  echo " -%"
else
  vol=$(pactl get-source-volume @DEFAULT_SOURCE@ |\
        head -n1 |\
        cut -d'/' -f2 |\
        sed 's/ *//g')
  echo " ${vol}%"
fi
