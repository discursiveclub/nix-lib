#!/usr/bin/env sh
pactl set-sink-mute @DEFAULT_SINK@ toggle
mute=$(pactl get-sink-mute @DEFAULT_SINK@)
if [ "${mute}" = "Mute: yes" ]; then
  notify-desktop -t 1000 -r 8322483232 "Volume muted"
else
  notify-desktop -t 1000 -r 8322483232 "Volume unmuted"
fi
